
docker run -d --name redis-redisearch -p 6379:6379 redislabs/redisearch:latest - Deprecated

docker run -d --name redisearch -p 6379:6379 redis/redis-stack-server:latest

Step 1: Activate to the python environment
python -m venv myenv2
cd C:\Users\Administrator\Makesh\Capstone\myenv2\Scripts
./activate

pip install -r requirements.txt

Step 2: Set up the source code location
$env:PYTHONPATH = 'C:\Users\Administrator\Makesh\Capstone\cap-02\rag_app'
Set-Location -Path 'C:\Users\Administrator\Makesh\Capstone\cap-02\rag_app'
pytest -q

Step 3: Set up the OPEN API key
$env:OPENAI_API_KEY = ''

Step 4: Run the ingestion pipeline script
python pdf_loader.py nvidia.pdf  - Test the text extraction from slides

python main.py --> process your PDF and populate the Redis vector store.

python main.py ingest  --> TO ingest and store

python main.py query "What is the Datacenter revenue for FY2024"  --> Verify the ingestion

Step 5: Extract the image from PDF
install the poppler
1. Download Poppler for Windows
Download the latest Poppler binary from:
https://github.com/oschwartz10612/poppler-windows/releases/
Extract the zip file to a folder, e.g., C:\poppler.
2. Add Poppler to your PATH
Open System Properties → Environment Variables.
Under "System variables", find and select Path, then click Edit.
Add the path to the bin folder inside your Poppler directory, e.g.:
C:\poppler\Library\bin
or
C:\poppler\bin
(depending on your extracted folder structure).

Slide creation
python pdf_loader.py  --> This is to create the slide images

Step 5: Run the Rag pipeline

Summary Creation (Text + Image)
python multi_modal_summary.py

Populate your Redis vectorstore with the summaries and image references for all slides
python store_summaries_to_redis.py  


#A script named vision_rag_query.py is now created. It uses a MultiVectorRetriever to fetch the top relevant slides (with summaries and images) from Redis, builds a multi-modal prompt, and sends both text and images to GPT-4o for a grounded answer.
 python vision_rag_query.py "Summary the Datacenter review of Q2 annd Q3 or the FY2024"
 python vision_rag_query.py "Summarize the Datacenter review of Q2 and Q3 for FY2024" --k 5 --prompt "You are a financial analyst. Given the following question: {question}, use the slides and images to provide a detailed answer."

#Custom parateers
python vision_rag_query.py "Summarize the Datacenter review of Q2 and Q3 for FY2024" --k 4 --cache-threshold 0.85 --prompt "You are a financial analyst. Given the following question: {question}, use the slides and images to provide a detailed answer."


# Step 6: StreamLit UI
streamlit run rag_app/streamlit_app.py


Redis commands
redis-cli FT.DROPINDEX slides-multimodal-index DD
redis-cli FLUSHALL
redis-cli KEYS "*"

